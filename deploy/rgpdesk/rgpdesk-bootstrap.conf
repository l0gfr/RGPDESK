# First installation only: ACME HTTP challenge, no application over HTTP.
<VirtualHost *:80>
    ServerName rgpdesk.fr
    DocumentRoot /var/www/rgpdesk-acme
    <Directory /var/www/rgpdesk-acme>
        Options -Indexes -ExecCGI -Includes -MultiViews -FollowSymLinks
        AllowOverride None
        Require all granted
    </Directory>
    RewriteEngine On
    RewriteCond %{REQUEST_URI} !^/\.well-known/acme-challenge/[A-Za-z0-9_-]+$
    RewriteRule ^ - [R=404,END]
    ErrorLog ${APACHE_LOG_DIR}/rgpdesk-error.log
    CustomLog /dev/null combined
</VirtualHost>
