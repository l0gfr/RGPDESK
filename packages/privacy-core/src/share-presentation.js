// Presentation uses only an already-validated, explicitly selected public DTO.
const escape = (v) => String(v).replace(/[&<>"']/g, (c) => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]);
const text = (k) => k.state === "documented" ? escape(k.value) : '<span class="unknown">À documenter</span>';
export function presentationSections(register) {
  const activity = (id) => id === null ? "Ensemble du périmètre" : register.activities.find((a) => a.id === id).title;
  const positions = register.positions.map((p) => '<li><div><small>' + escape(activity(p.activityId)) + '</small><h3>' + escape(p.position) + '</h3><p class="pre">' + escape(p.reason) + '</p></div></li>').join('');
  const tasks = register.nextSteps.map((a) => '<li><div><small>' + escape(activity(a.activityId)) + '</small><h3>' + escape(a.task) + '</h3><p>Suivi : ' + escape(a.owner || "À affecter") + '<br>Échéance déclarée : ' + escape(a.due || "À fixer") + '</p></div></li>').join('');
  const flows = register.flows.map((f, index) => '<figure><figcaption>Flux ' + (index + 1) + ' · ' + escape(activity(f.activityId)) + '</figcaption><div class="relationship">' + [['Origine',f.source],['Opération →',f.operation],['Destination',f.destination]].map(([label,k]) => '<div class="node"><h3>' + label + '</h3><p>' + text(k) + '</p></div>').join('') + '</div><dl class="facts">' + [['Données',f.data],['Canal',f.channel],['Pays et accès distants',f.location],['Habilitations',f.access]].map(([label,k]) => '<div><dt>' + label + '</dt><dd>' + text(k) + '</dd></div>').join('') + '</dl></figure>').join('');
  return '<section class="activity" aria-label="Synthèse de restitution"><p class="eyebrow">Pour décider et suivre</p><h2>Les positions et leurs suites.</h2><p class="quiet">Sélection du rédacteur pour ce destinataire. Les appréciations sont humaines et déclarées. Cette synthèse ne mesure pas la conformité.</p><h3>Positions communiquées</h3>' + (positions ? '<ul class="index">' + positions + '</ul>' : '<p>Aucune position sélectionnée pour ce partage.</p>') + '<h3>Suites communiquées</h3>' + (tasks ? '<ul class="index">' + tasks + '</ul>' : '<p>Aucune suite sélectionnée pour ce partage.</p>') + '</section><section class="activity" aria-label="Cartographie sélectionnée"><p class="eyebrow">Les mouvements déclarés</p><h2>Suivre les données.</h2><p class="quiet">Seuls les flux explicitement sélectionnés apparaissent ici. Leur absence ne démontre pas une absence de circulation.</p>' + (flows || '<p>Aucun flux sélectionné pour ce partage.</p>') + '</section>';
}

/** Optional first reading, bounded human text and already-selected actions only. */
export function executivePage(register) {
  const brief = (value) => Array.from(value).length>180 ? Array.from(value).slice(0,180).join('')+'…' : value;
  const action = (a) => '<li><div><h3>'+escape(brief(a.task))+'</h3><p>'+escape(a.owner||'Responsable à affecter')+' · '+escape(a.due||'Échéance à fixer')+'</p></div></li>';
  return '<section class="summary" aria-label="Synthèse direction"><p class="eyebrow">L’essentiel pour la direction</p><h2>Comprendre. Arbitrer. Agir.</h2><p class="quiet">Synthèse rédigée et sélectionnée pour '+escape(register.recipient)+'. Les réserves du dossier restent applicables.</p><dl class="facts"><div><dt>01 / Ce qui change</dt><dd class="pre">'+escape(register.executive.changes)+'</dd></div><div><dt>02 / Arbitrages demandés</dt><dd class="pre">'+escape(register.executive.arbitrations)+'</dd></div></dl><h3>03 / Prochaines actions</h3>'+(register.nextSteps.length?'<ul class="index">'+register.nextSteps.slice(0,3).map(action).join('')+'</ul>':'<p>Aucune action sélectionnée pour cette restitution.</p>')+(register.nextSteps.length>3?'<p>Les '+(register.nextSteps.length-3)+' autres actions choisies figurent dans les suites détaillées.</p>':'')+'<p class="quiet">Les intitulés longs sont abrégés ici ; les suites détaillées en conservent le texte complet. Ce résumé ne constitue pas une validation juridique. Retrouvez ensuite le périmètre, les réserves, les flux choisis et les fiches.</p></section>';
}
